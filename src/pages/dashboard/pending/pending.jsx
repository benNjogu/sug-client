import DefaultLayout from '../../../components/default-layout/default-layout.component';

const Pending = () => {
  return (
    <DefaultLayout>
      <div>Pending</div>
    </DefaultLayout>
  );
};

export default Pending;
